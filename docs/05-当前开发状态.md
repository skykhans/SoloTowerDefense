# 当前开发状态

更新日期：2026-03-10

## 当前已经完成

- 新建 `cocos-wechat/` 目录，作为 Cocos Creator + TypeScript + 微信小游戏主开发方向
- 已落首批 Cocos 脚本骨架：配置层、状态层、实体层、地图层、系统层
- 已补无后端本地存档方案：`LocalStorageService`、`SaveData`
- 已补首批 Cocos UI 骨架：`HomeUI`、`BattleUI`、`TowerPanel`、`ResultUI`
- 已补关卡配置与建造点管理：`LevelConfig`、`BuildSpotManager`
- 已补部署文档、MainScene 节点结构说明、预制体接入说明
- 已补主线一键启动脚本：根目录 `start.bat`、`start.ps1`
- 已补最小 Cocos 工程元文件，`cocos-wechat/` 现在可被 Dashboard 识别
- 已补建造点点击建塔入口：`BuildSpotClickHandler`
- 已补敌人移动系统与炮塔自动攻击系统：`EnemyMovementSystem`、`TowerCombatSystem`
- 已重构波次调度与战斗阶段：`WaveSpawnSystem`、`BattlePhase`
- 已打通建塔模式选择、开始波次、失败结算重开所需的 UI 回调入口
- 已补炮塔选择、升级、出售与目标波次通关判定
- 已补首页关卡选择与存档重置入口
- 已补首页关卡按钮状态反馈与结算页标题/提示文案
- 已补 `TowerPanel` 完整字段与按钮状态反馈
- 已补可执行的 Cocos 场景搭建清单
- 已补微信小游戏构建配置说明与发布前检查清单
- 已创建 `assets/scenes / prefabs / textures / audio` 资源目录占位
- 已新增 `platform/` 平台适配层并接入本地存储
- 已补第一批平台能力入口：系统信息、震动、分享
- 已将震动与分享入口接入首页、结算页、建塔与升级节点
- 已补开发期平台调试面板：`PlatformDebugPanel`
- 已补界面布局模式与安全区占位：`UILayoutService`、`SafeAreaPanel`
- 已接入微信 `safeArea` 与 Web 安全区探测，启动日志和调试面板会显示实际安全区边距
- 已补可复用布局控制器：`UILayoutController`，统一收口紧凑布局缩放与横竖屏差异
- 已补自适应布局组：`AdaptiveLayoutGroup`，用于容器横竖排切换、间距与内边距统一调整
- 已补布局预设：`UILayoutPresets`，用于为首页、战斗 HUD、结算面板提供默认适配参数
- 已补场景布局模板：`SceneLayoutProfiles`，用于统一记录三张主场景的推荐挂载点与预设组合
- 已补场景节点命名常量：`SceneNodeNames`，用于统一首页、战斗、结算场景的节点命名
- 已补场景结构模板与自检：`SceneStructureProfiles`、`SceneStructureValidator`
- 已补预制体命名、结构模板与自检：`PrefabNodeNames`、`PrefabStructureProfiles`、`PrefabStructureValidator`
- 已补预制体资源占位模板：`PrefabAssetProfiles`
- 已补场景资源占位模板与最小可运行资源清单：`SceneAssetProfiles`
- 已补统一资源清单入口：`ResourceManifest` + `ConfigService` 资源读取方法
- 已补资源清单校验器：`ResourceManifestValidator`
- 已补可直接运行的资源检查脚本：`tools/check-resource-manifest.ps1`
- 已落首批最小占位资源：`32` 个 PNG 占位图与 `6` 个 WAV 占位音频
- 已补基础新手引导状态与首局提示：`TutorialGuideService`
- 已补章节配置：`ChapterConfig`
- 已补关卡结算扩展：星级评价、首次通关奖励、关卡进度持久化
- 已补首页进度汇总与推荐关卡计算：`HomeProgressService`
- 已补结算回首页即时提示：`HomeNoticeSession`
- 已补结算回首页的关卡焦点联动：通关后自动定位新解锁关卡，失败后保留当前关卡
- 已补结算场景“下一关”入口：胜利后可直接进入已解锁的新关卡
- 已补第三关配置与章节归属，并开始引入护盾怪
- 已补分裂怪：死亡后会拆分出更快的小体型敌人
- 已补治疗怪：会周期性为附近敌人恢复生命值
- 已引入统一场景名、场景导航服务与跨场景结算会话
- 已补 `HomeScene` 与 `ResultScene` 节点结构说明
- 已新增场景 Installer 与绑定校验工具：`HomeSceneInstaller`、`MainSceneInstaller`、`ResultSceneInstaller`、`SceneBindingValidator`
- 已补表现层组件：`TowerView`、`BuildSpotView`
- 已补敌人表现层：`EnemyView`
- 已补子弹表现与命中特效占位：`BulletView`、`HitEffect`
- 旧版 Web 原型已迁移到 `legacy-web/`，避免与主开发方向混淆

## 本轮新增方向

- 将需求、技术选型、迁移方案、开发计划沉淀到 `docs/`
- 从旧 Web 原型切换到新的 Cocos 微信小游戏开发方向
- 将旧版原型移出根目录，降低误导
- 将场景接线从“文档约定”升级到“Installer + 校验器”模式
- 将炮塔高亮、范围圈、建造位状态拆分为独立表现组件
- 将敌人血条和类型外观拆分为独立表现组件
- 已为战斗 HUD 补充按钮状态反馈
- 已将安全区适配从“占位接口”推进到“真实 padding 数据 + Widget 偏移”模式
- 已将场景主体结构推进到“命名常量 + 布局模板 + 结构自检 + 绑定校验”闭环

## 当前仍缺少

- Cocos 场景文件与预制体
- 微信小游戏平台接入
- 本地存档与更多进度字段联动
- 配置编辑体验或轻量配置工具
- 章节与关卡结构
- 局外成长系统
- 更多特殊敌人
- 场景资源与视觉表现
- 炮塔与敌人的资源皮肤化

## 建议下一步

1. 在 Cocos Creator 中创建 `HomeScene / MainScene / ResultScene`
2. 将 Installer 挂到各场景根节点并完成 Inspector 绑定
3. 创建敌人、炮塔、建造点、子弹、命中特效预制体，并挂上对应 `View`/特效组件
4. 继续补首页局外成长入口、章节结构和更多特殊敌人
